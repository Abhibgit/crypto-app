<!DOCTYPE html>
<html>
  <head>
    <title><%= user.name %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= user.name %></h1>
    <p>Name <%= user.name %></p>
    <div>Cast:</div>
    <ul>
      <%- user.coin.map(c => `
      <li>${c.name} <small>${c.price}</small></li>
      ` ).join('') %>
    </ul>

    <!-- add to cast form below this comment -->
    <!-- <form id="add-per-to-coin" action="???" method="POST">
      <select name="coinId">
        <%- coins.map(c => `
        <option value="${c._id}">${c.name}</option>
        ` ).join('') %>
      </select>
      <button type="submit">Add to Coin</button>
    </form> -->
  </body>
</html>
